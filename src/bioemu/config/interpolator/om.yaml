_target_: bioemu.interpolate.interpolator.Interpolator
_partial_: true

# BioEmu does denoising from 0.99 to 0.001
latent_time: 0.05           # BioEmu ranges typically over some range near 0.01.
initial_guess_level: 0.2   # Initial guess level for interpolation

om_steps: 5000               # Number of optimization steps--actual was 2000
path_batch_size: -1         # -1 Turns off batch optimization – process the whole path

gamma: 0.1 # finally used to get zeta "gamma" set as samp_args.om_gamma * torch.tensor(masses).to(device) in the interpolator class
D: 2 # finally reset as samp_args.om_d / (trainset.std if args.scale_data else 1.0) ** 2 in the interpolator class
action_temp: 1 # Temperature parameter for the action distribution

optimizer:
  _target_: torch.optim.Adam
  _partial_: true
  lr: 0.001